obj BaseObj {
  value: i32
}

obj DerivedObj: BaseObj {
  value: i32,
  extra: i32
}

fn project(val: DerivedObj)
  match(val)
    BaseObj: val.value + val.extra

pub fn main()
  let derived = DerivedObj { value: 5, extra: 2 }
  project(derived)
