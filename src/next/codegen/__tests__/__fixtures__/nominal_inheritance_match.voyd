obj Base {
  value: i32
}

obj Derived: Base {
  value: i32,
  extra: i32
}

fn project(val: Derived)
  match(val)
    Base: val.value + val.extra

pub fn main()
  let derived = Derived { value: 5, extra: 2 }
  project(derived)
