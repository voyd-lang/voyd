pub fn overloaded(x: i32) -> i32
  x

pub fn overloaded(x: f64) -> f64
  x

fn choose<T>(value: T) -> T
  overloaded(value)

pub fn call_int() -> i32
  choose(1)

pub fn call_float() -> f64
  choose(1.0)
