fn fib(n: i32) -> i32
	if n <= 1 then:
		n
	else:
		fib(n - 1) + fib(n - 2)

fn main()
	let x = 10 +
		20 +
		30

	do me while () => if x > 10
		then:
			x -= 1
		else:
			x += 1

	let n = if args.len() > 1 then:
		console.log("Hey there!")
		args.at(1).parseInt().unwrap()
	else:
		10

	let result = fib(n)
