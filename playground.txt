
fn sorted(arr: Array[Int], by fn: Fn(Int, Int) Void) Void => {
    /** yada */
}

planets.sorted { a, b =>
    a > 1 ? 1 :
    b > 1 ? 2 :
    a > b ? 1 :
    2
}

struct Vec3 {
    let x, y, z: Int

    init() => {

    }
}
