use std::all

fn reduce<T>(ay: Array<T>, { start: T, reducer cb: (acc: T, current: T) -> T }) -> T
  let a = ay
  start

fn add<T>(a: T, b: T) -> T
  a + b

pub fn main() -> i32
  let x = 5
  [1, 2, 3]
    .reduce start: 0 reducer: (acc, current) =>
      acc + current
    .add(3)
  x + 3
