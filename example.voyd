
pub obj Version {
  api major: i32,
  api minor: i32,
  api patch: i32
}

impl Version
  fn init({ major: i32, minor: i32, patch: i32 })
    Version { major, minor, patch }

pub fn main()
  let v = Version(major: 1, minor: 2, patch: 3)
  v.major
