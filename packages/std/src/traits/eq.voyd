pub trait Eq<T>
  fn eq(self, { other: T }) -> bool

  fn ne(self, { other: T }) -> bool
    not self.eq(other: other)

  fn '=='(self, other: T) -> bool
    self.eq(other: other)

  fn '!='(self, other: T) -> bool
    self.ne(other: other)
