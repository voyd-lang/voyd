{
  "name": "@voyd/std",
  "version": "0.0.0",
  "private": true,
  "description": "Voyd standard library sources",
  "scripts": {
    "build": "echo 'standard library source bundle'",
    "typecheck": "echo 'no types for voyd std'",
    "test": "NODE_OPTIONS='--import tsx --conditions=development' voyd test ./src",
    "lint": "echo 'no lint configured'"
  }
}
