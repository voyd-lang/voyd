obj Some<T> {
  value: T
}

obj None {}

type Optional<T> = Some<T> | None

type Left = { x?: i32 }
type Right = { x?: bool }

fn program({ item: Left }) -> i32
  1

fn program({ item: Right, tag: i32 }) -> i32
  tag
