trait Parse
  fn parse(self, value: i32) -> i32
  fn parse(self, value: bool) -> i32

obj Box { value: i32 }

impl Parse for Box
  fn parse(self, value: i32) -> i32
    self.value + value

  fn parse(self, value: bool) -> i32
    if value then: self.value else: 0

fn use_qualified(p: Parse) -> i32
  Parse::parse(p, 2) + p.Parse::parse(false)

pub fn main() -> i32
  use_qualified(Box { value: 7 })
