trait Parse
  fn parse(self, value: i32) -> i32

  fn parse(self, value: bool) -> i32
    if value then: self.parse(1) else: 0

obj Box { value: i32 }

impl Parse for Box
  fn parse(self, value: i32) -> i32
    self.value + value

pub fn main() -> i32
  let b = Box { value: 4 }
  b.parse(true)
