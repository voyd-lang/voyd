trait Hashable
  fn hash(self) -> i32

obj Box<T> {
  value: T
}

impl<T> Hashable for Box<T>
  fn hash(self) -> i32
    1

impl Hashable for Box<i32>
  fn hash(self) -> i32
    2

fn main() -> i32
  let b = Box<i32> { value: 1 }
  b.hash()
