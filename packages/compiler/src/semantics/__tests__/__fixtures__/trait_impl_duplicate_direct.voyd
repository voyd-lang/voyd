trait Foo
  fn bar(self) -> i32

obj Baz {
  x: i32
}

impl Foo for Baz
  fn bar(self) -> i32
    self.x

impl Foo for Baz
  fn bar(self) -> i32
    self.x + 1

fn main() -> i32
  let b = Baz { x: 10 }
  b.bar()
