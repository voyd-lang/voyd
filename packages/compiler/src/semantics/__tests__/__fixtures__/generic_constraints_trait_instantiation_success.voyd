trait Animal
  fn score(self) -> i32

obj Dog {
  score_value: i32
}

impl Animal for Dog
  fn score(self) -> i32
    self.score_value

trait Carrier<T: Animal>
  fn carry(self, value: T) -> i32

obj DogCarrier {}

impl Carrier<Dog> for DogCarrier
  fn carry(self, value: Dog) -> i32
    value.score()

fn main() -> i32
  let carrier = DogCarrier {}
  carrier.carry(Dog { score_value: 7 })
