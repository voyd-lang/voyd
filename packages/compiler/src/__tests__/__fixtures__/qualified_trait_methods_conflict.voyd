trait Foo
  fn baz(self) -> i32

trait Bar
  fn baz(self) -> i32

obj X {
  x: i32
}

impl Foo for X
  fn baz(self) -> i32
    self.x + 10

impl Bar for X
  fn baz(self) -> i32
    self.x + 20

pub fn main()
  let x = X { x: 10 }
  Foo::baz(x) + x.Bar::baz()
