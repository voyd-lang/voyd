fn sum_pair({ left: i32, right: i32 }) -> i32
  left + right

fn each({ visit f: (value: i32) -> i32 }) -> i32
  f(1) + f(2) + f(3)

pub fn main() -> i32
  let base = 0
  each visit(value):
    sum_pair(left: base, right: value)
