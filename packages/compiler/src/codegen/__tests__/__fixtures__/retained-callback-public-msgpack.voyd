use std::msgpack::MsgPack
use std::msgpack::self as msgpack
use std::vx::retain_message_mapper

pub fn retain_public_msgpack_callback() -> i32
  retain_message_mapper((value: MsgPack) -> MsgPack => msgpack::make_string("ok"))
