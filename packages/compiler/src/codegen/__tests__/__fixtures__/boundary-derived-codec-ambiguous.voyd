use std::enums::{ enum }
use std::data::encode
use std::string::type::String

enum AmbiguousBoundaryValue
  Tagged { tag: String }
  Other

pub fn main() -> void
  let value: AmbiguousBoundaryValue = AmbiguousBoundaryValue::Tagged { tag: "payload" }
  encode(value)
  void
