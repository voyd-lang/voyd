use std::error::panic
use std::string::type::{ String, StringSlice }

pub fn require_contains(
  value: String,
  needle: StringSlice,
  message: String
) -> void
  if not value.contains(needle):
    panic(message)
