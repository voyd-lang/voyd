type Pair = (i32, i32)

fn consume_forward() -> i32
  let (left, right) = build_pair()
  combine((right, left))

fn combine(pair: Pair) -> i32
  let (a, b) = pair
  a * 10 + b

fn build_pair()
  (2, 3)

pub fn main() -> i32
  consume_forward()
