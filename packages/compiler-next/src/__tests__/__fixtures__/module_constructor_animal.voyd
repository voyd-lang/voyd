pub obj Animal {
  id: i32,
  name: i32,
}

impl Animal
  pub fn init({ name: i32 }) -> Animal
    Animal { id: 1, name }

  pub fn init(value: i32) -> Animal
    Animal { id: 2, name: value }

  pub fn label() -> i32
    7
