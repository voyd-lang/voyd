use std::log
use std::output::{ StdErr, write_line, write }
use std::number::all

fn fib(n: i32) -> i32
  if n < 2:
    n
  else:
    fib(n - 1) + fib(n - 2)

pub fn main()
  log::info("hi")
  fib(10).to_string().write_line()
  void
