use std::string::type::new_string
use std::vx::all

pub fn main()
  <div class="greeting" visible>
    Hi there
    <span class="big">hi</span>
    <i>This is italic</i>
  </div>

pub fn empty_children_with_shadows()
  let Array = 1
  let Html = 2
  <input />

pub fn option_attributes()
  <option value="voyd" selected={true}>Voyd</option>
