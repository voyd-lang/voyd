eff Async
  log(resume, msg: i32) -> i32

fn forward(callback: fn(i32) -> i32, value: i32) -> i32
  callback(value)

pub fn main() -> i32
  try
    Async::log(1)
  Async::log(resume, msg):
    forward(resume, msg)
