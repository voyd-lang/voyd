// Browser stub for `glob`. This should never run in the browser.
export const glob = async (..._args: any[]) => {
  throw new Error("glob is not available in the browser");
};

export default { glob };

