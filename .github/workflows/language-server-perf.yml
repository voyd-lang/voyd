name: Language Server Perf

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  ls-perf:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --include=optional
      - name: Run language server perf harness
        env:
          VOYD_USE_SRC: "1"
          VOYD_LS_PERF: "1"
          VOYD_LS_PERF_EDIT_RUNS: "5"
          VOYD_LS_PERF_MIN_SPEEDUP: "5"
          VOYD_LS_PERF_MIN_RSS_REDUCTION_PCT: "30"
          VOYD_LS_PERF_SCENARIOS: "100k:400:85,300k:1100:90"
          VOYD_LS_PERF_TIMEOUT_MS: "2400000"
        run: npm run --workspace @voyd/language-server test -- src/__tests__/analysis-coordinator.perf.test.ts
